#include <iostream>
using namespace std;
// int sortZeros(int arr[], int size)
// {
//     for (int i = 0; i < size; i++)
//     {
//         if (arr[i] < arr[i + 1])
//         {
//             int temp = arr[i];
//             arr[i] = arr[i + 1];
//             arr[i + 1] = temp;
//         }
//     }
//     cout << "the sorted array" << endl;
//     for (int i = 0; i < size; i++)
//     {
//         cout << arr[i] << " ";
//     }
// }

void sortZeroOne(int arr[], int n)
{
    int zeroCount = 0;
    int oneCount = 0;
    for (int i = 0; i < n; i++)
    {
    }
}
int main()
{
    int arr[] = {0, 1, 0, 1, 0, 1, 1, 0};
    int size = 8;
    // sortZeros(arr, size);
    // arr[].sort();

    return 0;
}